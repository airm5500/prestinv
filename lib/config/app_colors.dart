// lib/config/app_colors.dart

import 'package:flutter/material.dart';

class AppColors {
  // Couleurs basées sur la charte UbiPharm
  static const Color primary = Color(0xFF002D42);    // Bleu très foncé pour les AppBar
  static const Color accent = Color(0xFF00B09B);     // Bleu-vert vif pour les boutons et accents
  static const Color background = Color(0xFFF5F7FA); // Gris très clair pour le fond des écrans
  static const Color white = Colors.white;
  static const Color darkText = Colors.black87;
}
